# Run unit & integration tests
on: 
  push:
    paths-ignore:
      - 'docs/**'  # only trigger build if a file outside of /docs was changed
      - '_locales/*.xml'
      - '.vscode/**'
      - 'README_ASSETS/**'
      - 'README.md'

jobs:
  test:
    name: Tests and E2E Tests
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest] # macos-latest doesn't work currently: ERROR UnableToAccessScreenRecordingAPIError: The find-window process cannot access the Screen Recording API.
      fail-fast: false # don't fail fast, because some tests can be flaky so better we get all results
    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js 16.x
      uses: actions/setup-node@v1
      with:
        node-version: 16.x
    - name: npm ci
      run: |
        npm ci
    - name: npm run build
      run: |
        npm run build
    - name: npm test-unit
      run: |
        npm run test-unit
    - name: prepare testcafe
      run: |
        cd e2e_tests
        npm ci
    - uses: DevExpress/testcafe-action@latest
      working-directory: ./e2e_tests
      with:
        version: "2.6.2"
        args: "--skip-js-errors \"electron:../\" \"./testcafe/*\" --compilerOptions.typescript.configPath=./testcafe/tsconfig.json"
      env:
        DCC_NEW_TMP_EMAIL: ${{secrets.DCC_NEW_TMP_EMAIL}}
        NODE_ENV: test
        TEST_DIR: .test_tmp_data
