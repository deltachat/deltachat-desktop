{
  "private": true,
  "name": "@deltachat-desktop/shared",
  "type": "module",
  "scripts": {
    "check:types": "tsc --noEmit",
    "test": "esbuild *.ts --outdir=ts-compiled-for-tests --sourcemap && mocha 'tests/**/*.js'"
  },
  "dependencies": {
    "@deltachat/jsonrpc-client": "catalog:",
    "error-stack-parser": "^2.0.7",
    "stackframe": "^1.2.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.17",
    "@types/mocha": "^10.0.7",
    "chai": "^5.1.1",
    "esbuild": "^0.23.0",
    "mocha": "^10.7.0"
  }
}
